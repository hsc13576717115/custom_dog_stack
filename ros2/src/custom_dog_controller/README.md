# custom_dog_controller

This package owns observation construction, ONNX inference, action scaling and
safety limiting. The current revision freezes the v1.2 dimensions,
observation offsets and optional command/target calibration contract; it is not
yet a hardware controller executable.
