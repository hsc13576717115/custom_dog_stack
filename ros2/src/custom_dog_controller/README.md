# custom_dog_controller

This package owns observation construction, ONNX inference, action scaling and
safety limiting. The current revision only freezes the v1 policy dimensions and
offsets; it is not yet a hardware controller executable.
